- condition: evt.type = connect and container.id != host and fd.snet = 10.48.0.0/12
    and fd.sport = 8443
  desc: Audit connect to 10.48.0.0/12 (rule 1/10)
  output: Connection to monitored network 10.48.0.0/12 (command=%proc.cmdline dest=%fd.sip:%fd.sport
    container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 1
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.71.52.0/24
    and fd.sport = 6379
  desc: Audit connect to 10.71.52.0/24 (rule 2/10)
  output: Connection to monitored network 10.71.52.0/24 (command=%proc.cmdline dest=%fd.sip:%fd.sport
    container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 2
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.16.0.0/12
    and fd.sport = 8443
  desc: Audit connect to 10.16.0.0/12 (rule 3/10)
  output: Connection to monitored network 10.16.0.0/12 (command=%proc.cmdline dest=%fd.sip:%fd.sport
    container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 3
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.0.0.0/12
  desc: Audit connect to 10.0.0.0/12 (rule 4/10)
  output: Connection to monitored network 10.0.0.0/12 (command=%proc.cmdline dest=%fd.sip:%fd.sport
    container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 4
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.142.3.80/28
    and fd.sport = 6379
  desc: Audit connect to 10.142.3.80/28 (rule 5/10)
  output: Connection to monitored network 10.142.3.80/28 (command=%proc.cmdline dest=%fd.sip:%fd.sport
    container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 5
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.160.0.0/12
    and fd.sport = 5432
  desc: Audit connect to 10.160.0.0/12 (rule 6/10)
  output: Connection to monitored network 10.160.0.0/12 (command=%proc.cmdline dest=%fd.sip:%fd.sport
    container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 6
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.0.0.0/8
    and fd.sport = 5432
  desc: Audit connect to 10.0.0.0/8 (rule 7/10)
  output: Connection to monitored network 10.0.0.0/8 (command=%proc.cmdline dest=%fd.sip:%fd.sport
    container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 7
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.176.135.16/28
  desc: Audit connect to 10.176.135.16/28 (rule 8/10)
  output: Connection to monitored network 10.176.135.16/28 (command=%proc.cmdline
    dest=%fd.sip:%fd.sport container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 8
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.63.193.0/24
    and fd.sport = 3306
  desc: Audit connect to 10.63.193.0/24 (rule 9/10)
  output: Connection to monitored network 10.63.193.0/24 (command=%proc.cmdline dest=%fd.sip:%fd.sport
    container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 9
  tags:
  - bench
  - policy-scalability
- condition: evt.type = connect and container.id != host and fd.snet = 10.116.148.40/32
    and fd.sport = 8443
  desc: Audit connect to 10.116.148.40/32 (rule 10/10)
  output: Connection to monitored network 10.116.148.40/32 (command=%proc.cmdline
    dest=%fd.sip:%fd.sport container=%container.name ns=%k8s.ns.name)
  priority: NOTICE
  rule: Bench Network Rule 10
  tags:
  - bench
  - policy-scalability
